import { Routes } from '@angular/router';
import { HeroShellComponent } from './containers/hero-shell/hero-shell.component';

export const heroRoutes: Routes = [
  { path: '', component: HeroShellComponent }
];
